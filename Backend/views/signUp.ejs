<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>signUp</title>
	<!-- <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet"> -->
	<link
		rel="stylesheet"
		href="https://bootswatch.com/4/journal/bootstrap.min.css"
		/>
    <link rel="stylesheet" href="/Css/signUp.css">
</head>
<body>
   <div class="section-container">
      <div class="container">
        <div class="signup-img">

			<div class="signup-img-content">
				<h2>get started</h2>
				<p>Reach new milestones with a website specially made for you</p>
			</div>
		</div>
        <div class="form">
			<div class="form-header">
				<h3 style="font-family: sans-serif;">Sign Up</h3>
			</div>
            <form action="/users/register" method="POST">
				
				<%- include ("./partials/messages") %>

                <div class="first-label">
					
                    <label for="firstName" id="first">First Name
					<input
						type="name"
						name="firstName"
						placeholder="Enter First Name"
						value="<%= typeof firstName != 'undefined' ? firstName : '' %>"
					>
					</label>
					<label for="lastName">Last Name
						<input
							type="name"
							id="lastName"
							name="lastName"
							placeholder="Enter Last Name"
							value="<%= typeof lastName != 'undefined' ? lastName : '' %>"
						/>
					</label>
            	</div>
				<label for="email">Email
					<input
						type="email"
						id="email"
						name="email"
						placeholder="Enter Email"
						value="<%= typeof email != 'undefined' ? email : '' %>"
					/>
				</label>
				<label for="officeAddress">Office address
					<input
						type="text"
						id="name"
						name="officeAddress"
						placeholder="Enter Office Address"
						value="<%= typeof officeAddress != 'undefined' ? officeAddress : '' %>"
					/>
				</label>  
				<label for="typeOfBusiness">TOB
					<input
						type="text"
						id="name"
						name="typeOfBusiness"
						placeholder="Enter Type of Business"
						value="<%= typeof typeOfBusiness != 'undefined' ? typeOfBusiness : '' %>"
					/>  
				</label>  
				<label for="password">Password
					<input
						type="password"
						id="password"
						name="password"	
						placeholder="Create Password"
						value="<%= typeof password != 'undefined' ? password : '' %>"
					/>
				</label>
				<label for="password2">Confirm Password
					<input
						type="password"
						id="password2"
						name="password2"
						placeholder="Confirm Password"
						value="<%= typeof password2 != 'undefined' ? password2 : '' %>"
					/>
				</label>
				<div class="submit">
					<a class="signup-btn"><button style="border: none; background-color: inherit; color: inherit;" type="submit">Sign Up</button></a>
				</div>
			</form>				
			<p class="signup-paragraph">Already a member?<a href="/users/login"
				rel="noopener noreferrer"> Login</a> </p>
			</div>    
        </div>
       </div>
   </div>
</body>
</html>